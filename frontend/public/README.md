This folder is served statically by Vite at the project root `frontend/public`.

Place images, icons, and other static assets here. They will be available at `/` when running the dev server or after build.

Examples:
- `/logo.svg`
- `/images/device-placeholder.png`

Do not commit large binary assets if they are not required; prefer an asset CDN for production.
